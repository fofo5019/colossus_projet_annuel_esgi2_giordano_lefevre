{
  "name": "colossus-vitrine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npx serve . -p 8000",
    "build": "echo 'No build needed for vanilla HTML'",
    "preview": "npx serve . -p 8000"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}